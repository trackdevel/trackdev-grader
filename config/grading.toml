# REMOVED in Phase 6 — grading logic lives in config/grading.standard.json
# and is edited via the TrackDev Grader desktop app (apps/desktop).
# This stub remains only until the file is deleted from version control.
